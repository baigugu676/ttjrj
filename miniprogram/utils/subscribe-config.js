// 微信订阅消息模板配置
// 在微信公众平台「功能 → 订阅消息」申请模板后，将模板 ID 填入对应角色；
// 留空则该角色的订阅推送自动跳过（不影响任何现有功能）。
//
// 说明：
//   - 用户点击「订阅授权」后每次授权只能发送一条消息（一次性订阅）；
//   - 前端每次授权成功会调用 users.subscribeSelf 在云端记录一条额度；
//   - 云端每次发送成功后消耗一条额度。
module.exports = {
  TEMPLATE_IDS: {
    user: '',      // 报修用户：工单进度通知（审核结果/接单/验收结果）
    repairer: '',  // 维修人员：新任务提醒 / 验收结果通知
    admin: ''      // 管理员：新工单提醒 / 维修完成提醒
  }
};
