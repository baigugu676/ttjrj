# 监控状态可视化与维修历史改造说明

## 1. 目标

所有登录用户都可以查看监控设备的运行状态总览，并可选择任一监控查看当前状态、统计指标和完整维修历史。维修历史必须展示报修用户、维修人员、管理员以及每次动作的时间、角色和具体内容。

## 2. 术语与统计口径

- **监控**：现有 `locations` 点位记录（`device_type` 为摄像头、报警器等设备类型）。后续接口统一使用 `location`/“监控”文案，兼容现有字段。
- **正常**：该监控没有处于 `pending_review`、`pending_repair`、`repairing`、`pending_accept`、`repair_returned` 的未完成工单；最近一次有效验收结果为通过，或从未发生过故障。
- **损坏/故障中**：存在上述任一未完成工单。`rejected` 不计入当前故障。
- **已修复**：工单状态为 `completed`，仅用于历史统计，不代表当前故障。
- **正常率**：`正常监控数 / 启用监控总数 * 100%`，总数为 0 时返回 0%。
- **损坏率**：`当前故障监控数 / 启用监控总数 * 100%`，按监控去重，一个监控有多个未完成工单仍计 1 台。
- 所有统计接口只统计 `locations.status = active`；维修历史可查看该监控的全部工单，包括已驳回和已完成。

## 3. 权限

| 能力 | user | repairer | admin |
|---|:---:|:---:|:---:|
| 查看监控状态总览/圆环 | ✓ | ✓ | ✓ |
| 查看启用监控列表 | ✓ | ✓ | ✓ |
| 查看任意监控详情与维修历史 | ✓ | ✓ | ✓ |
| 创建/编辑/停用监控 |  |  | ✓ |
| 对工单执行审核、验收、删除 |  |  | ✓ |

详情读取不得沿用原工单的“报修人只能看自己的、维修人员只能看指派给自己的”限制；只要已登录即可读取监控维度的只读信息。写操作仍使用原有角色校验。

## 4. 后端改造

### 4.1 REST API（`backend/src/routes/locations.js`）

新增以下登录后可访问的接口，统一返回 `{ code: 0, data }`：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/locations/monitor-overview` | 返回圆环和总览统计 |
| GET | `/api/locations/monitor-status` | 返回启用监控列表，支持 `keyword`、`status` 筛选 |
| GET | `/api/locations/:id/monitor-detail` | 返回单个监控当前状态、指标、关联工单和动作时间线 |

`monitor-overview` 返回：

```json
{
  "total": 12,
  "normal": 9,
  "fault": 2,
  "repairing": 1,
  "completedOrders": 34,
  "normalRate": 75,
  "faultRate": 25,
  "segments": [
    { "key": "normal", "label": "正常", "value": 9, "color": "#16a34a" },
    { "key": "fault", "label": "故障中", "value": 2, "color": "#ef4444" },
    { "key": "repairing", "label": "维修中", "value": 1, "color": "#f59e0b" }
  ]
}
```

`monitor-status` 每项至少包含：`id`、`name`、`area`、`device_type`、`status`（`normal`/`fault`/`repairing`）、`statusText`、`openOrderCount`、`totalOrderCount`、`lastActionAt`。

`monitor-detail` 至少包含：

- 监控基础信息和当前状态；
- `metrics`：总工单数、已完成数、故障中工单数、最近维修时间、平均维修时长；
- `orders`：该监控的工单摘要（工单号、状态、报修人、维修人员、创建/更新时间）；
- `timeline`：按时间升序的动作记录，每条包含 `time`、`action`、`description`、`actorId`、`actorName`、`actorRole`、`orderId`。动作来源包括报修提交、管理员审核通过/驳回、维修人员接单、提交维修、管理员验收通过/退回、删除等。缺少历史动作时返回空数组，不得报错。

实现建议：复用 `work_orders`、`repair_records`、`acceptance_records` 和 `notifications`，必要时在查询层补充“submitted/updated”动作；不要改变既有工单状态流转。为避免 N+1，列表接口先批量读取工单再按 `location_id` 聚合。

### 4.2 云函数兼容（`cloudfunctions/locations` 或新增 `cloudfunctions/monitor-stats`）

小程序当前通过云函数调用业务。若前端未切换 REST，请提供同等 action：`monitorOverview`、`monitorStatus`、`monitorDetail`，返回字段与 REST 完全一致，并确保任意登录角色可读。优先复用已有 `locations`、`orders` 数据集合。

### 4.3 数据库与测试

- 如需索引，在 `backend/database/init.sql` 增加 `work_orders(location_id,status)`、`repair_records(order_id,created_at)` 等索引说明。
- 增加接口测试：未登录拒绝；三种角色均可读；正常率/损坏率按监控去重；详情时间线包含用户、维修人员、管理员动作；无历史数据返回空数组。

## 5. 小程序前端改造

### 5.1 首页/监控总览

在所有角色可进入的首页增加“监控状态”入口和总览区：

- 圆环图（使用 Canvas 或现有组件能力，不引入体积过大的依赖）展示正常/故障中/维修中数量；
- 圆环中心显示启用监控总数；
- 圆环下方使用明确表格/数据行展示：正常数、故障数、维修中数、正常率、损坏率；
- 点击任一统计项或“查看全部监控”进入监控列表；
- 加载中、空数据、接口失败均有可见状态和重试入口。

### 5.2 新增监控列表页 `pages/monitor/list`

- 所有已登录用户可访问；支持按名称/区域搜索和按状态筛选；
- 每行展示监控名称、区域、设备类型、当前状态、故障工单数、最近动作时间；
- 点击行进入详情；列表支持下拉刷新和触底分页（若后端提供分页）。

### 5.3 新增监控详情页 `pages/monitor/detail`

- 顶部展示监控基础信息和醒目的当前状态；
- 指标表格展示总工单、已完成、故障中、平均维修时长、最近维修时间；
- “维修历史”按时间倒序展示工单摘要和动作时间线；
- 每条动作明确显示操作者姓名、角色（报修用户/维修人员/管理员）、动作、时间、备注/维修措施/验收意见；
- 无历史、加载、错误状态可用；不提供写操作，写操作仍从原有工单页面进入。

### 5.4 路由与公共工具

- 在 `miniprogram/app.json` 注册两个新页面；从首页和必要的工单页面提供入口。
- 在 `miniprogram/utils/api.js` 增加三个 API 封装，或在云函数封装中增加三个 action；统一处理鉴权、错误提示和分页。
- 在 `miniprogram/utils/util.js` 增加监控状态文案、角色文案、百分比和日期格式化辅助函数。
- 样式沿用现有全局颜色和卡片规范，圆环颜色固定为正常绿、故障红、维修中橙，保证文字不重叠。

## 6. 两个智能体的并行分工

### Agent A：后端与数据

- 实现 REST/云函数的三个只读接口；
- 聚合统计与动作时间线，补充索引；
- 编写并运行后端接口测试；
- 不修改小程序页面文件，完成后说明接口字段和验证命令。

### Agent B：小程序前端

- 实现首页总览、圆环与统计表格；
- 新增监控列表和详情页、路由、API/工具封装；
- 编写可运行的前端自测或静态校验；
- 不修改后端业务文件，完成后说明依赖的接口字段。

### 主智能体联调

- 合并双方改动，解决接口字段或命名冲突；
- 执行 `npm test`/项目已有测试与静态检查；
- 检查三种角色权限、空数据、错误重试和移动端布局；
- 更新本说明中的完成状态和遗留风险。

## 7. 验收标准

1. 任一登录角色打开首页即可看到监控总数、正常率、损坏率以及正常/故障中/维修中圆环分布。
2. 任一登录角色可搜索并选择监控，查看当前状态和指标表格。
3. 监控详情维修历史同时出现报修用户、维修人员、管理员的动作，包含姓名、角色、时间和动作内容。
4. 统计按监控去重，状态变化后刷新可得到一致结果；无工单监控显示正常。
5. 未登录无法访问；管理员原有增删改和工单写权限不受影响。
6. 既有报修、维修、验收流程回归测试通过。

## 8. 完成记录

- [x] 后端接口与测试（REST、云函数 `monitorOverview`/`monitorStatus`/`monitorDetail`、数据库索引；现有后端测试通过）
- [x] 小程序总览、列表、详情
- [x] 联调与回归测试（后端现有测试通过，前端完成脚本语法与路由静态校验）
- [x] 文档字段/遗留风险更新

### 8.1 实现备注

- 小程序通过 `utils/api.js` 的 `getMonitorOverview`、`getMonitorStatus`、`getMonitorDetail` 调用 `locations` 云函数；REST 与云函数返回同一字段结构。
- 首页 Canvas 圆环固定使用正常绿、故障红、维修中橙；列表支持名称/区域/设备类型搜索、状态筛选、下拉刷新；详情时间线以倒序呈现并显示操作者姓名、角色、时间和动作内容。
- 统计查询仅读取启用监控，云函数兼容历史记录中仅保存用户 ID 的数据并回填姓名。当前云端查询上限为每次 1000 条，超大规模部署时需补充分页聚合。
