# 设备故障报修与维修跟踪系统 — 开发任务分工文档

---

## 项目概述

一个**微信小程序**承载三个角色（管理员、报修用户、维修人员）。用户微信授权登录后，根据角色自动切换功能界面。

**业务流程**：报修提交 → 管理员审核派单 → 维修人员接单维修（拍照+GPS+原因+措施）→ OK上线 → 管理员验收归档

---

## 技术选型

| 层 | 技术 |
|---|------|
| 前端 | 微信小程序原生开发 |
| 后端 | Node.js + Express |
| 数据库 | MySQL 8.0 |
| 认证 | JWT |
| 图片 | Multer + 本地存储 (uploads/) |

---

## 分工总览

```
┌─────────────────────────────────────────────────────────┐
│                    项目根目录                            │
│          d:\Fenpeirenwu_And_shangchuan\                 │
├────────────────────────┬────────────────────────────────┤
│      backend/          │       miniprogram/             │
│    (Agent 1 负责)       │      (Agent 2 负责)             │
├────────────────────────┼────────────────────────────────┤
│  • 数据库表设计         │  • 小程序配置                   │
│  • RESTful API         │  • 登录/授权页                  │
│  • JWT 认证            │  • TabBar 三tab                 │
│  • 文件上传            │  • 14个业务页面                  │
│  • 数据校验            │  • 3个复用组件                   │
│  • 统计查询            │  • 微信API封装                   │
└────────────────────────┴────────────────────────────────┘
```

---

# Agent 1：后端 API 服务

## 目录：`backend/`

## 1. 项目文件清单

```
backend/
├── package.json              # 依赖配置
├── .env                      # 环境变量
├── database/
│   └── init.sql              # 建表 + 预置数据
└── src/
    ├── app.js                # Express 主入口
    ├── config/
    │   └── db.js             # MySQL 连接池
    ├── middleware/
    │   └── auth.js           # JWT 验证 + 角色校验
    ├── routes/
    │   ├── auth.js           # 登录认证
    │   ├── users.js          # 用户管理
    │   ├── locations.js      # 点位管理
    │   ├── orders.js         # 工单CRUD + 状态流转
    │   ├── upload.js         # 图片上传
    │   ├── statistics.js     # 数据统计
    │   └── notifications.js  # 消息通知
    └── utils/
        ├── generateOrderNo.js  # 工单号生成
        └── notify.js           # 通知发送辅助函数
```

## 2. 数据库表

> 所有建表SQL写入 `database/init.sql`，包含预置测试数据

### 2.1 users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) UNIQUE | 登录用户名 |
| password_hash | VARCHAR(255) | bcrypt 加密 |
| openid | VARCHAR(100) | 微信 openid |
| real_name | VARCHAR(50) | 真实姓名 |
| role | ENUM('admin','user','repairer') | 角色 |
| phone | VARCHAR(20) | 手机号 |
| avatar_url | VARCHAR(500) | 头像URL |
| status | ENUM('active','disabled') DEFAULT 'active' | 状态 |
| created_at | DATETIME DEFAULT NOW() | |
| updated_at | DATETIME DEFAULT NOW() ON UPDATE | |

**预置数据：**

| username | password(明文→存hash) | role | real_name |
|----------|----------------------|------|-----------|
| admin | admin123 | admin | 管理员A |
| zhangsan | 123456 | user | 张三 |
| lisi | 123456 | user | 李四 |
| repairer1 | 123456 | repairer | 维修员小刘 |
| repairer2 | 123456 | repairer | 维修员老陈 |

### 2.2 locations（点位表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| name | VARCHAR(100) | 点位名称 |
| area | VARCHAR(100) | 所属区域 |
| device_type | VARCHAR(50) | 设备类型 |
| sort_order | INT DEFAULT 0 | 排序 |
| status | ENUM('active','inactive') DEFAULT 'active' | |
| created_at | DATETIME | |

**预置数据：**

| name | area | device_type |
|------|------|-------------|
| 3号楼道摄像头-01 | 3号楼 | 摄像头 |
| 大门入口摄像头-03 | 大门 | 摄像头 |
| 停车场摄像头-02 | 停车场 | 摄像头 |
| 2号楼道摄像头-01 | 2号楼 | 摄像头 |
| 围墙报警器-05 | 围墙 | 报警器 |

### 2.3 work_orders（工单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| order_no | VARCHAR(20) UNIQUE | 工单号 WO20260801001 |
| reporter_id | INT FK→users | 报修人 |
| location_id | INT FK→locations | 点位 |
| fault_description | TEXT | 故障描述 |
| repair_requirements | TEXT | 维修要求（选填）|
| status | ENUM('pending_review','pending_repair','repairing','pending_accept','completed','rejected','repair_returned') | 状态 |
| assigned_repairer_id | INT FK→users | 指派的维修人 |
| reviewer_id | INT FK→users | 审核人 |
| review_comment | TEXT | 审核意见 |
| reject_reason | TEXT | 驳回/退回原因 |
| reviewed_at | DATETIME | 审核时间 |
| created_at | DATETIME | |
| updated_at | DATETIME | |

### 2.4 order_images（工单图片表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| order_id | INT FK→work_orders | |
| image_url | VARCHAR(500) | 图片路径 |
| image_type | ENUM('report','repair_before','repair_after') | 图片类型 |
| uploader_id | INT FK→users | |
| sort_order | INT DEFAULT 0 | |
| created_at | DATETIME | |

### 2.5 repair_records（维修记录表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| order_id | INT FK | |
| repairer_id | INT FK→users | |
| start_time | DATETIME | 维修开始时间 |
| end_time | DATETIME | OK上线时间 |
| gps_latitude | DECIMAL(10,7) | GPS纬度 |
| gps_longitude | DECIMAL(10,7) | GPS经度 |
| location_address | VARCHAR(500) | 地址描述 |
| fault_reason | TEXT | 故障原因分析 |
| repair_action | TEXT | 维修措施描述 |
| created_at | DATETIME | |

### 2.6 acceptance_records（验收记录表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| order_id | INT FK | |
| reviewer_id | INT FK→users | |
| result | ENUM('pass','return') | |
| return_reason | TEXT | |
| accepted_at | DATETIME | |

### 2.7 notifications（通知表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| user_id | INT FK→users | 接收人 |
| order_id | INT FK | 关联工单 |
| type | VARCHAR(50) | 通知类型 |
| title | VARCHAR(200) | 标题 |
| content | TEXT | 内容 |
| is_read | TINYINT DEFAULT 0 | |
| created_at | DATETIME | |

---

## 3. API 接口清单

> **统一返回格式**：`{ code: 0, message: 'success', data: ... }`
> **错误格式**：`{ code: 1, message: '错误描述' }`
> **分页格式**：`{ code: 0, data: { list: [...], total: 100, page: 1, pageSize: 20 } }`

### 3.1 认证模块 `routes/auth.js`

| 方法 | 路径 | 认证 | 说明 |
|------|------|:---:|------|
| POST | `/api/auth/login` | ❌ | 用户名密码登录，返回 token + 用户信息 |
| GET | `/api/auth/me` | ✅ | 获取当前登录用户信息 |

**POST /api/auth/login 请求体：**
```json
{ "username": "zhangsan", "password": "123456" }
```

**POST /api/auth/login 返回：**
```json
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOi...",
    "user": { "id": 3, "real_name": "张三", "role": "user", "avatar_url": "" }
  }
}
```

### 3.2 用户管理 `routes/users.js`（全部需要 admin 角色）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/users` | 用户列表，支持 `?role=` 筛选 |
| POST | `/api/users` | 创建用户 |
| PUT | `/api/users/:id` | 编辑用户 |
| PUT | `/api/users/:id/status` | 启用/禁用 `{status:'active'\|'disabled'}` |
| DELETE | `/api/users/:id` | 删除用户 |

### 3.3 点位管理 `routes/locations.js`

| 方法 | 路径 | 认证 | 权限 | 说明 |
|------|------|:---:|------|------|
| GET | `/api/locations` | ✅ | 所有角色 | 点位列表 |
| POST | `/api/locations` | ✅ | admin | 添加点位 |
| PUT | `/api/locations/:id` | ✅ | admin | 编辑点位 |
| DELETE | `/api/locations/:id` | ✅ | admin | 删除点位 |

### 3.4 工单管理 `routes/orders.js`（核心模块）

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/orders` | 所有角色 | **创建工单**（自动生成order_no，状态=pending_review）|
| GET | `/api/orders` | 按角色过滤 | **工单列表**，支持 `?status=&reporter_id=&assigned_repairer_id=&page=&pageSize=` |
| GET | `/api/orders/:id` | 按角色 | **工单详情**（含关联图片、维修记录、验收记录）|
| PUT | `/api/orders/:id/review` | admin | **审核操作** |
| PUT | `/api/orders/:id/accept-repair` | repairer | **接单** |
| PUT | `/api/orders/:id/repair` | repairer | **提交维修记录** |
| PUT | `/api/orders/:id/accept` | admin | **验收操作** |
| DELETE | `/api/orders/:id` | admin | 删除工单 |

**各接口请求体说明：**

```
POST /api/orders
{
  "location_id": 1,
  "fault_description": "画面黑屏无法显示",
  "repair_requirements": "尽快修复"  // 选填
}

PUT /api/orders/:id/review
{
  "action": "approve",           // "approve" | "reject"
  "assigned_repairer_id": 4,     // approve时必填
  "review_comment": "确认属实",   // 选填
  "reject_reason": "照片不清晰"   // reject时必填
}

PUT /api/orders/:id/repair
{
  "start_time": "2026-08-01 14:30:00",
  "gps_latitude": 30.2500,
  "gps_longitude": 120.1200,
  "location_address": "XX市XX区XX路XX号",
  "fault_reason": "电源适配器损坏",
  "repair_action": "更换12V/2A电源适配器"
}

PUT /api/orders/:id/accept
{
  "action": "pass",               // "pass" | "return"
  "return_reason": "照片不清晰"   // return时必填
}
```

### 3.5 图片上传 `routes/upload.js`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload` | 上传单张图片，form-data: `file` + `order_id` + `image_type` |

### 3.6 数据统计 `routes/statistics.js`（需 admin 角色）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/statistics/overview` | 概览数据（今日新增/待处理/本月完成/平均时长）|
| GET | `/api/statistics/status-distribution` | 各状态工单数量 |
| GET | `/api/statistics/trend` | 近30天趋势（每天新增/完成数）|
| GET | `/api/statistics/location-ranking` | 点位故障排行 Top10 |
| GET | `/api/statistics/repairer-workload` | 维修人员工作量 |

### 3.7 消息通知 `routes/notifications.js`（需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/notifications` | 当前用户通知列表 |
| PUT | `/api/notifications/:id/read` | 标记单条已读 |
| PUT | `/api/notifications/read-all` | 全部标记已读 |
| GET | `/api/notifications/unread-count` | 未读数量 |

---

## 4. 关键中间件

### `middleware/auth.js`

```js
// authMiddleware — 从 Authorization: Bearer <token> 提取并验证JWT
// 验证通过后挂载 req.user = { id, role, real_name }

// requireRole(...roles) — 角色校验工厂函数
// 用法: router.post('/xxx', authMiddleware, requireRole('admin'), handler)
```

---

## 5. 状态流转规则

```
pending_review ──审核通过──▶ pending_repair ──接单──▶ repairing
      │                          │                       │
      └──驳回──▶ rejected        │                提交维修记录
                                 │                       │
                                 │                       ▼
                                 │              pending_accept
                                 │                  │
                                 │         验收通过  │  退回维修
                                 │                  ▼       │
                                 │             completed    │
                                 │                          │
                                 │              repair_returned
                                 │                    │
                                 └── 重新审核 ◀───────┘（维修人员重新提交后）
```

**校验规则：**
- 只有 `pending_review` 状态可以审核
- 只有 `pending_repair` 状态可以接单
- 只有 `repairing` 状态可以提交维修
- 只有 `pending_accept` 状态可以验收

---

## 6. 辅助工具

### `utils/generateOrderNo.js`
生成规则：`WO` + 年月日 + 3位自增序号（从001开始，当天去重查询最大值+1）
示例：`WO20260801001`

### `utils/notify.js`
导出函数，在工单状态变更时自动插入通知记录：

```js
notifyOrderStatusChange(order_id, action, order_no, location_name)
// action: 'submitted' → 通知管理员
// action: 'approved' → 通知报修用户 + 维修人员
// action: 'rejected' → 通知报修用户
// action: 'accepted_repair' → 通知报修用户
// action: 'repair_done' → 通知管理员
// action: 'accepted' → 通知维修人员
// action: 'returned' → 通知维修人员
```

---

## 7. 依赖

```json
{
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1"
  }
}
```

---

## 8. 环境变量 `.env`

```
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=repair_system
JWT_SECRET=repair_system_jwt_secret_2026
UPLOAD_DIR=uploads
```

---

# Agent 2：微信小程序前端

## 目录：`miniprogram/`

## 1. 项目文件清单

```
miniprogram/
├── project.config.json       # 小程序项目配置
├── app.json                  # 全局配置 + 页面路由 + TabBar
├── app.js                    # 小程序入口（登录逻辑/globalData）
├── app.wxss                  # 全局样式
├── utils/
│   ├── api.js                # HTTP 请求封装
│   └── util.js               # 工具函数
├── components/
│   ├── order-card/           # 工单卡片组件
│   │   ├── order-card.wxml
│   │   ├── order-card.wxss
│   │   ├── order-card.js
│   │   └── order-card.json
│   └── timeline/             # 时间线组件
│       ├── timeline.wxml
│       ├── timeline.wxss
│       ├── timeline.js
│       └── timeline.json
├── pages/
│   ├── login/                # 登录页
│   │   └── login.{wxml,wxss,js,json}
│   ├── index/                # 首页（按角色动态展示）
│   │   └── index.{wxml,wxss,js,json}
│   ├── report/
│   │   ├── create/           # 新建报修
│   │   ├── list/             # 我的报修列表
│   │   └── detail/           # 报修详情（时间线）
│   ├── repair/
│   │   ├── pool/             # 待维修工单池
│   │   ├── mytasks/          # 我的维修任务
│   │   └── execute/          # 维修执行（核心页面）
│   ├── admin/
│   │   ├── orders/           # 全部工单管理
│   │   ├── order-detail/     # 工单详情（前后对比+验收）
│   │   ├── statistics/       # 数据统计看板
│   │   ├── users/            # 用户管理
│   │   └── locations/        # 点位管理
│   └── mine/
│       └── index/            # 我的（个人中心）
└── images/                   # 静态资源（占位）
```

## 2. 全局配置

### `app.json` 要点

```json
{
  "pages": [
    "pages/login/login",
    "pages/index/index",
    "pages/report/create",
    "pages/report/list",
    "pages/report/detail",
    "pages/repair/pool",
    "pages/repair/mytasks",
    "pages/repair/execute",
    "pages/admin/orders",
    "pages/admin/order-detail",
    "pages/admin/statistics",
    "pages/admin/users",
    "pages/admin/locations",
    "pages/mine/index"
  ],
  "tabBar": {
    "list": [
      { "pagePath": "pages/index/index", "text": "首页", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/report/list", "text": "工单", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/mine/index", "text": "我的", "iconPath": "...", "selectedIconPath": "..." }
    ]
  }
}
```

> **注意**：首页和工单tab的内容根据角色动态切换。维修人员的工单tab指向 `pages/repair/mytasks`，需在 page onShow 中根据角色做 `wx.redirectTo`

### `app.js` 要点
- `globalData`: token, userInfo, role
- `onLaunch`: 检查 Storage 中的 token，有效则自动登录
- `checkLogin()`: 无token或过期 → 跳转登录页

## 3. 工具层

### `utils/api.js`

```js
// 封装 wx.request，统一处理：
// - baseURL: http://localhost:3000/api
// - header 自动带 token
// - 自动 JSON.stringify data
// - 统一错误提示
// 导出: api.get(url, params), api.post(url, data), api.put(url, data), api.del(url)
// 导出: api.upload(filePath, formData)  →  wx.uploadFile 封装
```

### `utils/util.js`

```js
// formatTime(date, format)     → 日期格式化
// getStatusText(status)        → 状态码转中文
// getStatusColor(status)       → 状态对应颜色
// previewImages(urls, current) → 调用 wx.previewImage
```

## 4. 页面详细说明

### 4.1 `pages/login/login` — 登录页

**功能：**
- 微信一键登录按钮
- 静默获取 wx.login code
- 后端换取 token
- 存储到 Storage 和 globalData
- 根据 role 跳转首页

**UI：**
- 居中布局
- 顶部应用名称 + 图标
- 微信登录按钮（绿色大按钮）

### 4.2 `pages/index/index` — 首页

**按角色显示不同内容：**

| 角色 | 展示内容 |
|------|---------|
| **报修用户** | 大按钮"➕ 新建报修"、统计卡片（待审核/待维修/维修中/已完成 数量）、最近3条报修 |
| **维修人员** | 统计卡片（待接单/维修中/今日完成）、待接单工单预览列表、维修中工单入口 |
| **管理员** | 统计卡片（待审核/待维修/待验收/本月完成）、快捷入口网格（审核/验收/统计/用户/点位管理）|

### 4.3 `pages/report/create` — 新建报修

**表单字段：**

| 字段 | 组件 | 必填 | 说明 |
|------|------|:---:|------|
| 点位名称 | `<picker>` | ✅ | 从 /api/locations 加载选项 |
| 故障描述 | `<textarea>` | ✅ | placeholder: "请详细描述故障现象..." |
| 现场照片 | 自定义上传 | ❌ | 最多9张，网格展示，支持预览/删除 |
| 维修要求 | `<textarea>` | ❌ | placeholder: "特殊要求、紧急程度等备注..." |

**提交逻辑：**
1. 先逐张上传图片 → 获取 image_id 数组
2. 调用 POST /api/orders → 成功后跳转列表页

### 4.4 `pages/report/list` — 我的报修列表

**功能：**
- 顶部状态筛选 tab（全部/待审核/待维修/维修中/待验收/已完成）
- 工单卡片列表（使用 order-card 组件）
- 下拉刷新、上拉加载更多
- 点击跳转详情页

### 4.5 `pages/report/detail` — 报修详情

**内容区域：**
- 工单基本信息（编号、点位、状态标签）
- 报修照片展示（可点击预览）
- **时间线组件**：提交→审核→接单→维修→验收（当前状态高亮）
- 报修人提交的原始信息

### 4.6 `pages/repair/pool` — 待维修工单池（维修人员）

**功能：**
- 待接单工单列表（status=pending_repair）
- 每单显示：点位、故障描述、提交时间
- "接单"按钮 → 确认弹窗 → PUT /api/orders/:id/accept-repair
- 下拉刷新

### 4.7 `pages/repair/mytasks` — 我的维修任务

**功能：**
- 顶部 tab：维修中 / 待验收 / 已完成
- 工单列表（使用 order-card 组件）
- 点击"维修中"的工单 → 进入维修执行页
- 下拉刷新

### 4.8 `pages/repair/execute` — 维修执行页 ⚠️ 核心页面

**页面结构（从上到下）：**

```
┌─ 报修信息区 ──────────────────────────┐
│  点位：xxx   故障描述：xxx              │
│  [查看报修照片]                        │
└──────────────────────────────────────┘

┌─ 步骤1：维修前拍照 * ─────────────────┐
│  [拍照按钮] → wx.chooseImage({camera}) │
│  [照片网格] 至少1张                    │
└──────────────────────────────────────┘

┌─ 步骤2：维修时间 * ───────────────────┐
│  自动填当前时间，可手动修改             │
└──────────────────────────────────────┘

┌─ 步骤3：维修地点 * ───────────────────┐
│  [GPS定位按钮] → wx.getLocation        │
│  显示经纬度 + 地址描述                  │
└──────────────────────────────────────┘

┌─ 步骤4：故障原因 * ───────────────────┐
│  [多行输入框] placeholder: 分析故障原因 │
└──────────────────────────────────────┘

┌─ 步骤5：维修措施 * ───────────────────┐
│  [多行输入框] placeholder: 描述维修操作 │
└──────────────────────────────────────┘

┌─ 步骤6：维修后拍照 * ─────────────────┐
│  [拍照按钮] → wx.chooseImage({camera}) │
│  [照片网格] 至少1张                    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│                                      │
│     ✅  确  认  OK  上  线            │
│                                      │
└──────────────────────────────────────┘
```

**提交逻辑：**
1. 先上传维修前/后照片
2. 收集所有表单数据
3. 调用 PUT /api/orders/:id/repair
4. 成功后跳转任务列表

### 4.9 `pages/admin/orders` — 工单管理（管理员）

**功能：**
- 顶部状态筛选 tab
- 工单列表（工单号、点位、报修人、状态、时间、操作按钮）
- 操作按钮根据状态变化：待审核→[审核] / 待验收→[验收] / 其他→[查看]
- 搜索框（按工单号或点位）
- 下拉刷新、上拉加载

### 4.10 `pages/admin/order-detail` — 工单详情（管理员）

**内容区域：**
- 报修信息区（点位、报修人、故障描述、照片、要求）
- 审核信息区（审核人、审核时间、审核意见、指派维修人）
- **维修前后照片并排对比区**（左侧维修前 / 右侧维修后）
- 维修信息区（维修人、时间、GPS位置、故障原因、维修措施）
- 时间线
- **验收操作区**：通过按钮 / 退回按钮（仅 pending_accept 状态显示）

### 4.11 `pages/admin/statistics` — 数据统计

**内容：**
- 4个数字卡片（今日新增、待处理、本月完成、平均时长）
- 状态分布（可用CSS/Cavnas画简易饼图或横向条形图）
- 点位故障排行 Top10 列表
- 维修人员工作量列表

### 4.12 `pages/admin/users` — 用户管理

**功能：**
- 用户列表（头像、姓名、角色标签、状态、操作）
- 按角色筛选
- 添加用户（弹窗表单：用户名、密码、姓名、角色、手机号）
- 编辑 / 禁用 / 启用

### 4.13 `pages/admin/locations` — 点位管理

**功能：**
- 点位列表
- 添加点位（弹窗：名称、区域、设备类型）
- 编辑 / 删除

### 4.14 `pages/mine/index` — 我的

**功能：**
- 顶部：头像、姓名、角色标签
- 功能菜单（按角色显示）：
  - 报修用户：我的报修
  - 维修人员：维修记录
  - 管理员：用户管理、点位管理、数据统计
- 底部：退出登录按钮

## 5. 复用组件

### 5.1 `components/order-card/` — 工单卡片

**props:** `order` 对象

**显示：**
- 左侧：工单号、点位名称、故障描述（1行截断）、提交时间
- 右侧：状态标签（不同颜色）
- 点击触发 `bindtap` 事件

### 5.2 `components/timeline/` — 时间线

**props:** `steps` 数组 `[{title, time, active}]`

**显示：**
- 左侧竖线 + 圆点
- 右侧标题 + 时间
- active=true 的节点高亮（蓝色圆点 + 加粗文字）

## 6. 全局样式变量 `app.wxss`

```css
/* 主题色 */
--primary: #07C160;      /* 微信绿 */
--danger: #FA5151;       /* 红色 */
--warning: #FFC300;      /* 黄色 */
--info: #10AEFF;         /* 蓝色 */
--text: #333333;
--text-secondary: #999999;
--bg: #F5F5F5;
--white: #FFFFFF;

/* 通用按钮 */
.btn-primary    /* 绿色主按钮 */
.btn-danger     /* 红色危险按钮 */
.btn-default    /* 白色默认按钮 */
.btn-ok         /* OK上线大按钮（醒目的渐变绿） */

/* 通用卡片 */
.card           /* 白色圆角卡片 + 阴影 */

/* 状态标签 */
.status-tag.pending_review  /* 橙色 */
.status-tag.pending_repair  /* 蓝色 */
.status-tag.repairing       /* 青色 */
.status-tag.pending_accept  /* 紫色 */
.status-tag.completed       /* 绿色 */
.status-tag.rejected        /* 红色 */
.status-tag.repair_returned /* 红色 */

/* 表单 */
.form-item       /* 表单项容器 */
.form-label      /* 标签 */
.form-input      /* 输入框 */
.form-textarea   /* 多行输入 */
.form-required   /* 必填星号 */
```

## 7. API 接口调用对照表

| 页面 | 调用的接口 |
|------|-----------|
| login | POST /api/auth/login |
| index | GET /api/orders?status=&page= / GET /api/statistics/overview |
| report/create | GET /api/locations + POST /api/upload + POST /api/orders |
| report/list | GET /api/orders?reporter_id=&status=&page= |
| report/detail | GET /api/orders/:id |
| repair/pool | GET /api/orders?status=pending_repair&page= |
| repair/mytasks | GET /api/orders?assigned_repairer_id=&status=&page= |
| repair/execute | PUT /api/orders/:id/accept-repair + POST /api/upload + PUT /api/orders/:id/repair |
| admin/orders | GET /api/orders?status=&page= |
| admin/order-detail | GET /api/orders/:id + PUT /api/orders/:id/accept |
| admin/statistics | GET /api/statistics/* |
| admin/users | GET /api/users + POST /api/users + PUT /api/users/:id |
| admin/locations | GET /api/locations + POST /api/locations |

---

## 8. 开发顺序建议

| 优先级 | Agent 1（后端） | Agent 2（小程序） |
|:---:|------|------|
| 1 | 数据库 init.sql + db.js | project.config.json + app.json + app.js + app.wxss |
| 2 | app.js + auth 中间件 + auth 路由 | utils/api.js + util.js |
| 3 | users + locations 路由 | login 页 + index 页 |
| 4 | orders 路由（核心） | report/* 页面 |
| 5 | upload 路由 | repair/* 页面（特别是 execute） |
| 6 | statistics 路由 | admin/* 页面 |
| 7 | notifications 路由 | mine 页 + 组件完善 |
| 8 | 整体测试 | 联调测试 |

---

> 📅 文档版本：v1.0
> 📅 创建日期：2026-08-01
> 🤖 供 Agent 1（后端）和 Agent 2（小程序前端）并行开发使用
