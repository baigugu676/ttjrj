# 代码优化任务清单

> 生成时间：2026-08-17
> 分析范围：backend（Node.js + Express + MySQL）、cloudfunctions（微信云函数）、miniprogram（微信小程序）
> 本轮实施：标记为 ✅ 的条目（高优先级 + 安全性修复）；标记为 ⏳ 的条目留待后续迭代。

---

## 一、本轮已实施的优化（✅）

### A. 后端 backend

| # | 问题 | 位置 | 严重程度 | 修复方案 | 状态 |
|---|------|------|:---:|------|:---:|
| A1 | JWT 只信任签发时的 role，管理员降权后旧 token 最长 7 天内仍持有全部管理权限 | `src/middleware/auth.js` | 高 | 回查数据库时同时取 `role, status`，以数据库实时角色覆盖 token 中的 role（并入原有 30 秒状态缓存） | ✅ |
| A2 | 启动时无 JWT_SECRET 校验，缺失时登录接口 500；无 .env 模板 | `src/server.js` | 高 | 启动时校验 JWT_SECRET 存在且长度 ≥ 32，否则 fail-fast 退出；新增 `.env.example` | ✅ |
| A3 | work_orders 缺 created_at 索引，工单列表排序、按日统计全表扫描 | `database/init.sql` | 高 | 新增 `idx_created_at(created_at, id)`、`idx_reporter_created(reporter_id, created_at)`、`idx_repairer_created(assigned_repairer_id, created_at)` | ✅ |
| A4 | acceptance_records 缺 (result, accepted_at) 索引，月度完成数/趋势统计全表扫描 | `database/init.sql` | 高 | 新增 `idx_result_accepted(result, accepted_at)` | ✅ |
| A5 | `DATE(created_at) = CURDATE()` 对索引列套函数导致无法走索引 | `src/routes/statistics.js`、`src/routes/orders.js` | 高 | 改写为 `created_at >= CURDATE() AND created_at < CURDATE() + INTERVAL 1 DAY` 范围条件 | ✅ |
| A6 | CSV 导出未中和公式前缀（= + - @），存在 CSV 注入风险 | `src/routes/orders.js` | 中 | esc() 对以 `= + - @ \t \r` 开头的单元格前置单引号 | ✅ |
| A7 | 连接池 connectionLimit 硬编码、无 pool error 事件监听 | `src/config/db.js` | 中 | 连接数改环境变量 `DB_POOL_LIMIT`（默认 10）；注册 `pool.on('error')` 日志；补充 maxIdle/idleTimeout | ✅ |
| A8 | 管理员通知循环逐条 INSERT 且串行 await；每次通知回查工单表 | `src/utils/notify.js` | 中 | 通知函数支持直接传入 order 对象避免回查；管理员通知改为单条多值 INSERT 批量写入 | ✅ |
| A9 | express.json 上限 10mb 偏大、响应头暴露 x-powered-by | `src/app.js` | 低 | JSON 上限降至 1mb；`app.disable('x-powered-by')` | ✅ |

### B. 云函数 cloudfunctions

| # | 问题 | 位置 | 严重程度 | 修复方案 | 状态 |
|---|------|------|:---:|------|:---:|
| B1 | 监控概览全量拉取所有 completed 工单（只增不减的集合）仅为计数 | `dashboard/index.js`、`orders/index.js` | 高 | 改为数据库端 `.count()`，仅按启用点位过滤时用聚合按点位分组计数 | ✅ |
| B2 | "今日完成"链路全量拉验收记录 + 工单表分块**串行**查询 | `dashboard/index.js`、`orders/index.js` | 高 | 分块之间无依赖，改为 `Promise.all` 并行 | ✅ |
| B3 | 用户列表全量拉取后内存过滤分页，且未做字段投影（password_hash 读入内存） | `users/index.js` | 中 | 加 `.field()` 投影，敏感字段不再离开数据库；keyword 过滤与分页下沉为 db.RegExp + skip/limit | ✅ |
| B4 | init 鉴权 catch 吞掉所有错误，临时故障可绕过"仅管理员"检查 | `init/index.js` | 中 | catch 中按错误信息确认是"集合不存在"才放行，其余错误返回失败 | ✅ |
| B5 | 通知发送循环内逐条串行 add | `orders/index.js` | 中 | 改为 `Promise.all` 并行写入 | ✅ |
| B6 | init/package.json 声明 bcryptjs 但代码未使用，徒增部署包体积 | `init/package.json` | 低 | 删除未使用依赖 | ✅ |

### C. 小程序 miniprogram

| # | 问题 | 位置 | 严重程度 | 修复方案 | 状态 |
|---|------|------|:---:|------|:---:|
| C1 | token 过期无统一处理，过期后所有请求报错且永远不会被引导回登录页 | `utils/api.js` | 高 | callCloud 集中识别鉴权失败错误码，清除本地会话并 reLaunch 登录页 | ✅ |
| C2 | wx.loading 为全局单例，并发请求先完成者会隐藏其他在途请求的 loading | `utils/api.js` | 中 | 模块内维护 loading 引用计数，归零才 hideLoading | ✅ |
| C3 | upload() 无超时控制，上传无响应时提交按钮永久卡死 | `utils/api.js` | 中 | 增加 timeout 包裹，超时 reject 并提示 | ✅ |
| C4 | 通知页修改单条已读状态却 setData 整个列表 | `pages/notifications/notifications.js` | 中 | 改为 `this.setData({ ['list[N].is_read']: true })` 局部更新 | ✅ |
| C5 | extractList 相同 4 行函数复制 9 份 | 8 个页面 js | 中 | 抽取到 `utils/util.js`，各页面统一引用 | ✅ |
| C6 | 角色守卫代码块复制 8 份（toast + setTimeout + switchTab） | 8 个页面 js | 中 | 抽取 `guardRole(role)` 到 `utils/util.js` | ✅ |
| C7 | statistics 页 5 个子请求全部 `.catch(() => ({}))`，断网时页面静默显示全 0，错误 UI 永不触发 | `pages/admin/statistics/statistics.js` | 中 | 记录子请求失败标记，全部失败才置 loadError，部分失败给降级提示 | ✅ |

---

## 二、留待后续迭代的优化（⏳）

### 后端（按优先级排序）

1. **监控接口全量加载历史工单**（`routes/locations.js` loadMonitorData）：对全部启用点位拉取全部工单（无状态过滤/无 LIMIT）+ 3 个 LEFT JOIN，keyword 过滤在 Node 内存进行。建议改为 SQL 聚合（GROUP BY location_id + MAX(updated_at)）并只取非终态工单。改动面大，需配合回归测试。
2. **JWT_SECRET 弱密钥**：本地 `.env` 中为可猜测字符串（`repair_system_jwt_secret_2026`），需运维侧用 `openssl rand -hex 32` 更换（.env 不入库，本轮已加启动强度校验兜底）。
3. **CORS 白名单**：未配置 `CORS_ORIGINS` 时全开，生产部署需配置环境变量收紧。
4. **/uploads 静态目录无鉴权**：报修照片可被未登录用户访问，建议改为鉴权路由流式返回。
5. **登录限速为单机内存版**：依赖 req.ip 且未配置 trust proxy，Nginx 后部署时攻击者爆破 admin 会锁定代理 IP|admin 键造成 DoS；建议计数落库或 Redis，并返回 429。
6. **CSV 导出无 LIMIT 全量载入内存**：建议游标分页 + res.write 流式输出。
7. **工单号 SELECT MAX+INSERT 竞态**：单日超 9999 单后创建全部失败，建议改原子 INSERT...SELECT 或独立计数表。
8. **statusMap / 工单 JOIN SQL 重复 5 处**：建议抽 constants/queryBuilders 公共模块。
9. **affectedRows 语义误用**：UPDATE 值未变化时误报"通知不存在"等，建议用 changedRows 或先查存在性。
10. **FOR UPDATE 未包事务**：orders.js 中 SELECT FOR UPDATE 经自动提交执行，锁形同虚设，建议删除或补事务。

### 云函数

1. **dashboard 云函数与 orders 的 dashboard action 整体双实现**：四个功能两套代码靠注释约定保持一致，建议保留一个数据源另一处转调。
2. **getCurrentUser/fetchAll/ok/fail 重复 6-7 份**：建议抽公共模块（部署脚本同步复制）。
3. **statistics 平均维修时长全量拉取**：建议改数据库端聚合（aggregate + lookup）。
4. **monitorDetail 全量拉取点位全部工单**：建议 field 投影 + 限制最近 N 条。
5. **login 无失败次数限制**：可暴力破解，建议按 username 记录失败次数临时锁定。
6. **getOrder/findUser catch 返回 null**：数据库故障被伪装成"工单不存在/用户名或密码错误"，应区分 errCode。

### 小程序

1. **列表页分页 loadList 模板重复 6 次**（含同一个 reset 竞态 bug 复制 6 份）：建议抽 createListLoader 工厂或 Behavior，并推广 monitor/list 已有的 _requestId 守卫。
2. **onShow 每次全量拉取**：首页/统计页/我的页每次切 tab 重发请求，建议参考 monitor/list 的 30 秒缓存 + 首次加载跳过策略。
3. **图片未加 lazy-load**：详情页多图场景建议补充。
4. **弃用 API**：wx.getSystemInfoSync / wx.createCanvasContext，建议迁移新接口。
5. **test 目录会打进发布包**：project.config.json 建议配置 packOptions.ignore。
6. **console.error 残留 18 处**：建议统一日志封装或发布前清理。
7. **create 页下拉刷新无反馈**：onPullDownRefresh 同步立即 stopPullDownRefresh。

---

## 三、验证方式与结果

- 所有改动 JS 文件通过 `node --check` 语法校验（24 个文件全部通过）；
- 后端测试套件 `npm test`（8 项断言）全部通过（修复过程中发现并纠正了一处 auth 中间件变量作用域问题）；
- 新增索引已应用到本地存量数据库（可通过 `SHOW INDEX FROM work_orders` 验证）；
- 云函数改动保持原有返回结构（code/message/data）不变；
- 小程序改动保持页面 data 字段与 WXML 绑定不变。

## 四、索引变更执行说明（已入库的存量库）

对已初始化过的数据库，手工执行以下 DDL（init.sql 已同步更新，新库自动生效）：

```sql
ALTER TABLE work_orders
  ADD KEY idx_created_at (created_at, id),
  ADD KEY idx_reporter_created (reporter_id, created_at),
  ADD KEY idx_repairer_created (assigned_repairer_id, created_at);

ALTER TABLE acceptance_records
  ADD KEY idx_result_accepted (result, accepted_at);
```
